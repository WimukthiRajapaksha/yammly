import 'package:flutter/material.dart';

class ShoppingPantryScreen extends StatelessWidget {
  const ShoppingPantryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
